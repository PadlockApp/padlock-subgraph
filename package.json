{
  "name": "padlock-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ PadlockApp/Padlock",
    "create-local": "graph create --node http://localhost:8020/ PadlockApp/Padlock",
    "remove-local": "graph remove --node http://localhost:8020/ PadlockApp/Padlock",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 PadlockApp/Padlock"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
